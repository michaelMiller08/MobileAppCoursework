<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeApp"
        tools:targetApi="31">
        <activity
            android:name=".Activites.ManageProducts.CustomEditProductDialog"
            android:theme="@android:style/Theme.Holo.Dialog"
            android:exported="false" />
        <activity
            android:name=".Activites.ManageProducts.ManageProductsActivity"
            android:exported="false" />
        <activity
            android:name=".Activites.Basket.BasketActivity"
            android:exported="false" />
        <activity
            android:name=".Activites.Landing.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".Activites.Landing.LandingActivity"
            android:exported="false"
            android:label="@string/title_activity_landing"
            android:theme="@style/Theme.CafeApp.ActionBar" />
        <activity
            android:name=".Activites.Register.RegisterActivity"
            android:exported="false"
            android:label="@string/title_activity_register"
            android:theme="@style/Theme.CafeApp" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>